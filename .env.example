# Public frontend endpoint for the Cloudflare Worker (no secrets here).
# The Worker hosts both /api/coach/* and /api/sandbox/*.
VITE_COACH_API_URL=https://robot-foundry-coach.hieudo831.workers.dev

# Optional local override while running `pnpm worker:dev`.
# VITE_COACH_API_URL=http://127.0.0.1:8787
